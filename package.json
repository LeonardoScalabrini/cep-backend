{
  "name": "cep-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "NODE_ENV=test nyc --all --check-coverage --lines=92 mocha test/ --recursive --reporter progress --exit"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.6.0",
    "express": "~4.16.1",
    "mongoose": "^6.0.5",
    "sinon": "^9.2.4"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.1",
    "nyc": "^15.1.0",
    "supertest": "^6.1.6"
  }
}
