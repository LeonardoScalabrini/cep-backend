{
  "name": "cep-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "NODE_ENV=test nyc --all --check-coverage --lines=90 mocha test/ --recursive --reporter progress --exit"
  },
  "dependencies": {
    "body-parser": "^1.19.2",
    "dotenv": "^8.6.0",
    "express": "~4.18.0",
    "helmet": "^5.0.2",
    "mongoose": "^6.2.6",
    "sinon": "^9.2.4"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.2",
    "nyc": "^15.1.0",
    "supertest": "^6.2.2"
  }
}
