{
  "name": "cep-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=dev node server.js",
    "test": "NODE_ENV=test nyc --all --reporter=lcov mocha test/ --exit --recursive --reporter progress",
    "sonar": "sonar-scanner"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "mongoose": "^5.10.15",
    "sinon": "^9.2.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "sonarqube-scanner": "^2.8.1",
    "supertest": "^6.0.1"
  }
}
